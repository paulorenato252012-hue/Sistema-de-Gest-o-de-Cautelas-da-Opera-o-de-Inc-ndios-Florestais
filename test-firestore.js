import { initializeApp } from "firebase/app";
import { getFirestore, collection, addDoc, serverTimestamp } from "firebase/firestore";
// Need auth to test this, so maybe this is not easy to run in the terminal.
